CODE SEGMENT
ASSUME CS:CODE, DS: DATA
START: MOV AX, DATA
MOV DS, AX
 MOV AL, LSBDIVIDENT
 MOV AH, MSBDIVIDENT
 MOV DH,DIVISOR
AAD
DIV DH
LEA BX,TABLE
LEA SI, ASCIIQUOTIENT
XLAT
MOV [SI], AL
MOV AH,09H
LEA DX,MESSAGE1
INT 21H
LEA SI, ASCIIREMAINDER
MOV AL, AH
XLAT
MOV [SI], AL
MOV AH,09H
LEA DX, MESSAGE2
INT 21H
QUIT: MOV AL,0
 MOV AH,04CH
INT 21H
CODE ENDS
END START

